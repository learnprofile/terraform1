{
  "storage": {
    "name": "#{storage_name}",
    "sak": "#{storage_sak}"
  },
  "iot": {
    "name": "#{iot_name}",
    "service_sap": "#{iot_service_sap}",
    "eh_name": "#{iot_eh_name}",
    "eh_endpoint": "#{iot_eh_endpoint}",
    "device": {
      "id": "#{iot_device_id}",
      "connection_string": "#{iot_device_connection}"
    }
  }
}